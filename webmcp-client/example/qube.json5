{
  "$schema": "https://q64.dev/schema/qube.json5",
  "name": "dev.q64.tool_caller",
  "version": "0.0.0",
  "license": "MIT OR Apache-2.0",
  "description": "Lists and invokes WebMCP tools using dev.q64.webmcp_client.",
  "type": "application",
  "entry": "main.q",

  // Local-path dependency on the qube this example lives under. The qube CLI
  // normalises this to an absolute path and links it via
  // `q64 --module dev.q64.webmcp_client=<path>/src`, exactly the way a
  // registry-resolved dependency is linked (spec/qube-cli.md).
  "dependencies": {
    "dev.q64.webmcp_client": { "path": ".." },
  },

  "targets": {
    "web": { "host": "browser" },
  },

  "effects": {
    "declared": ["@ui", "@stdout"],
  },
  "capabilities": ["Ui", "Stdout"],
}
